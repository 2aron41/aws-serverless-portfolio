# Day 50 — Terraform-Controlled Lambda Rollback Rehearsal

**Date:** August 25, 2026

## Objective

Prove the production Lambda rollback path end-to-end by moving the
Terraform-managed `live` alias from version 2 to version 1, verifying real
production traffic on version 1, and then restoring version 2 through a
separately reviewed Terraform plan.

## Starting Point

Day 50 began after the legitimate version-2 deployment completed.

Production baseline:

- Lambda versions: 1 and 2
- current alias: `live -> 2`
- Terraform resources: 31
- Terraform serial: 19
- four inquiry alarms: 4/4 OK
- normal Terraform convergence: No changes

Version 1 was now a real immutable older rollback target.

## Rollback Control Audit

Terraform already supported:

`inquiry_lambda_alias_version`

When unset:

`live`

follows the freshly published Lambda version.

When explicitly set to a numbered version, Terraform pins `live` to that
version.

Existing module tests already covered:

- follow-latest alias behavior
- explicit rollback override behavior
- rejection of invalid alias versions

## Disposable Rollback Design

The first simulation was intentionally plan-only.

A CLI quoting mistake initially produced the literal malformed value:

`"\"1\""`

Terraform validation correctly rejected it.

No AWS configuration changed.

The corrected simulation produced exactly:

`live: 2 -> 1`

with:

`0 add / 1 change / 0 destroy`

Lambda code, API integration, and invoke permission were all no-op.

The disposable design plan was deleted.

## Production tfvars Tracking Audit

An attempted tracked edit exposed that:

`infra/environments/prod/terraform.tfvars`

is intentionally ignored by Git through:

`*.tfvars`

The repository tracks:

`terraform.tfvars.example`

but not the real environment-specific production tfvars.

The temporary local rollback value was therefore restored from:

`"1"`

to:

`null`

The restored production tfvars matched the known baseline SHA256:

`2c567809f8d8fdf7b99fcc6250d62e4549a4775aef6a15929c9cdbd87a111519`

## Safe Rollback Artifact

Instead of force-adding the ignored production tfvars, Day 50 added:

`infra/environments/prod/rollback-v1.tfvars.example`

It explicitly contains:

`inquiry_lambda_alias_version = "1"`

The file is:

- tracked
- reviewable
- non-secret
- not auto-loaded by Terraform
- activated only with an explicit `-var-file`

Rollback artifact SHA256:

`8568beb0692ad8e31dd34b0339f3e4796522e9c9e541fed18ead0c0ad8762e41`

## Rollback Artifact Commit and CI

Commit:

`a00ff66dc38a52208d7d45c61e2aae10417cea84`

Commit scope:

- exactly one file

Exact-commit production Terraform CI:

- SUCCESS

Validate Portfolio Website:

- SUCCESS

Normal production configuration remained converged with No changes.

## Pre-Commit Verification

Before committing the artifact:

- module tests: 32/32
- production Terraform test: 1/1
- Terraform validation: PASS
- normal plan: No changes
- explicit artifact plan: alias only
- transition: `2 -> 1`
- production remained `live -> 2`
- state remained 31 resources / serial 19

## Rollback Saved Plan

Fresh post-CI rollback plan SHA256:

`c58fe6dc1e77dd919bc85cee9a0faed9b648e7090836dd4b4f30664adbefc763`

Exact plan:

`live: 2 -> 1`

Plan summary:

`0 add / 1 change / 0 destroy`

No Lambda package, API integration, or permission changes were present.

## Production Rollback

After all final gates, Terraform applied the exact reviewed saved plan.

Result:

`0 added / 1 changed / 0 destroyed`

Production became:

`live -> 1`

The applied rollback plan was deleted.

## Version-1 Verification

One controlled invalid request was sent through production.

Response:

- HTTP 400
- `{"message": "Invalid inquiry request."}`

CloudWatch confirmed:

`Version: 1`

API Gateway continued routing through the `live` alias.

Versions 1 and 2 remained immutable.

Terraform state became:

- resources: 31
- serial: 20

All four alarms remained OK.

## Restoration Plan

While production remained on version 1, a fresh normal-configuration saved
plan was generated.

Restore plan SHA256:

`ecbb2d6f977a9bc6364f4ef3790c1503e967dac912f0919ef7a5c18a9872d6b2`

Exact plan:

`live: 1 -> 2`

Plan summary:

`0 add / 1 change / 0 destroy`

Lambda code, API integration, and alias permission remained no-op.

No restore apply occurred until this separate saved plan was reviewed.

## Production Restoration

After final restoration gates, Terraform applied the exact saved restore plan.

Result:

`0 added / 1 changed / 0 destroyed`

Production returned to:

`live -> 2`

The applied restore binary plan was deleted.

## Version-2 Verification

A second controlled invalid request was sent.

Response:

- HTTP 400
- `{"message": "Invalid inquiry request."}`

CloudWatch confirmed:

`Version: 2`

The version-2 privacy-safe validation warning was observed.

## Final State

Terraform:

- resources: 31
- serial: 21
- lineage unchanged
- convergence: No changes

State progression:

`19 -> 20 -> 21`

Monitoring:

- inquiry alarms: 4/4 OK
- alarm actions: enabled

Production:

`API Gateway -> live -> version 2`

Rollback target:

`version 1`

Repository:

- commit:
  `a00ff66dc38a52208d7d45c61e2aae10417cea84`
- clean
- synchronized

## Production Requests

Exactly two controlled production requests were used during the complete
rehearsal:

1. rolled-back version 1 verification
2. restored version 2 verification

## Day 50 Result

The complete rollback lifecycle is now proven:

`v2 -> v1 -> v2`

Rollback is:

- Terraform-controlled
- alias-only
- explicit
- reviewable
- reproducible
- tested in real production
- restorable through a separate reviewed plan

Version 1 remains available as the known-good rollback target after the
rehearsal.

## Next

Preserve this evidence and ensure the operational rollback runbook reflects
the now-proven procedure and no longer describes version 1 as unavailable.

Future rollback operations should continue using:

`-var-file=rollback-v1.tfvars.example`

with the same safety process:

- verify identity and state
- verify immutable version inventory
- generate a fresh saved plan
- require alias-only mutation
- review and hash the saved plan
- apply that exact plan
- verify production functionality
- monitor alarms
- generate a separately reviewed restoration plan when appropriate
